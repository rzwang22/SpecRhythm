# B128 local validation record

Baseline: clean local/remote Draft PR5 HEAD
`e2dd4357099880c736bc30ee00a12b1886be5688`. No reset, GPU run, server connection,
historical-result mutation or other PR modification.

Production-path tests use real capacity_for, loaded-capacity report construction,
drive/clock/drain, native producer hooks, summarize, exact Bash measurement gate,
export and archive re-reading. CPU GPU adapters provide explicitly synthetic device
values; they do not establish actual B128 fit, outputs or native overlap. Actual
owner/controller/resident-scheduler tests are parameterized over B64 and B128 and
exercise physical worker batches, query positions, K3 candidate counts and both
resident audits. Existing source-contract tests still use pinned vLLM source.

First development run failures are retained in `/tmp/k3-b128-first.log`: the new
entry test initially passed strict-output to an exploration report; insufficient-
capacity injection initially contradicted its raw worker snapshot; a warmup test
omitted committed_tokens; and the historical report fixture always picked the
first home. The fixture now models the real owner's home preference for B128 so
its 256-opportunity coverage is meaningful. One unchanged B64 subprocess entry hit
its 20s timeout; its cause was not established. No timeout/assertion was relaxed.
The next new-test run only retained the missing committed_tokens fixture failure,
which is corrected by supplying actual row accounting to warmup_step.

Final commands/results and actual CI state are recorded on delivery, without
converting CPU passes or an earlier isolated rerun into GPU validation.

The added real CLI-summary check reached its geometry assertions, then three
Python3.11 exact-entry subprocesses hit the existing 20s limit
(`/tmp/k3-b128-summary.xml`). A sampled earlier local pytest process was waiting
in file reads; that observation does not establish the cause of these child
timeouts. A separate current acceptance-module import completed, which also does
not explain the timed-out runs. These failures are recorded, not treated as passed
by retrying. No budget or required evidence check was changed.

## Local verification (before fixed-entry delivery)

- Full Python 3.11: **2989 passed / 6 failed / 3 skipped** (966.368s).
  Three unchanged `test_k3_actual_joint_first_failure_and_export[True-...]`
  cases (Serial, Serial-eager, PingPong) and three unchanged
  `test_runtime_pair_preserve_first_error_stop_and_parent_shell` cases
  (capacity, evidence, evidence+exports) exceeded their existing 20s child limit.
  No protocol/geometry assertion failed in that run. This is not an all-green suite.
- Python 3.9 affected suites: **206 passed** (42.735s).
- Python 3.12 affected suites: **204 passed / 2 failed** (249.326s). The two
  new exact-entry Serial/Serial-eager performance tests exceeded the same 20s
  child limit. The failures are retained, not reclassified as baseline failures.
- Final configuration/export/native negative checks after the last guard changes:
  **12 passed**. The separate quantile/reverse-entry additions passed 2 cases.
- The same exact measurement gate on a retained real CPU-produced B128 Serial
  report completed under Python3.12 with its unchanged 20s limit. An import-time
  trace showed acceptance imports around 50ms in that separate invocation. These
  observations do not identify the intermittent child-timeout root cause and do
  not replace the failed suite results. No retries were added to production or tests.
- Ruff, compileall under Python3.9/3.11/3.12, Python3.9 AST compatibility, repository
  Bash syntax and `git diff --check` passed. Fixed-entry checks are recorded below
  after pinning the execution commit.

Local first failures and XML summaries are preserved under `/tmp/k3-b128-*`.
CI is assessed separately at the pushed SHA; a CI pass cannot retroactively
explain these local failures. Actual A100 capacity, native geometry, cleanup,
overlap and performance are PENDING. Full-output equivalence is intentionally NOT_RUN.

## Pinned entry

Execution `f7bfb43152f5655edbad9888c53c0a81e9d11954` contains both B128 and B64
runners plus scale-aware capacity/measurement/export code (verified with git objects).
The actual pinned Bash launcher passed8 CPU tests: default B128 unified, B128
io-only and explicit B64, successful/failing external runner, invalid selections,
original exit23 retained, stale private flags cleared and exactly one upload line.
One new overlong test-decorator Ruff finding was corrected without changing behavior.

The pinned-entry8 cases also passed separately under Python3.9 and3.12. No GPU
runner was invoked: only its external boundary was substituted. Fixed-entry commit
`23e748c1a1c093984a9b0abdab92d643f71b2624` pins the verified implementation objects.

CI snapshot after ordinary push of the fixed entry: both push/PR pinned-vLLM
source-contract checks passed; six remaining checks were still running. No CI
failure was observed at that snapshot, and incomplete checks are not declared PASS.
A later local compileall sample also caught a read() wait, then completed successfully;
it corroborates that local I/O stalls exist, not that all timeout causes are known.
Final CI status is reported against the actual delivery HEAD separately.
