diff --git a/vllm/v1/worker/gpu_model_runner.py b/vllm/v1/worker/gpu_model_runner.py
index e9b23f1..96162ed 100644
--- a/vllm/v1/worker/gpu_model_runner.py
+++ b/vllm/v1/worker/gpu_model_runner.py
@@ -4334,0 +4335,15 @@ class GPUModelRunner(
+        if (
+            self.speculative_config is not None
+            and self.speculative_config.method == "custom_class"
+            and scheduler_output.scheduled_spec_decode_tokens
+        ):
+            verify_start_hook = getattr(
+                self.drafter, "on_target_verify_start", None
+            )
+            if verify_start_hook is not None:
+                verify_start_hook(
+                    request_ids=self.input_batch.req_ids.copy(),
+                    scheduled_spec_token_ids=(
+                        scheduler_output.scheduled_spec_decode_tokens.copy()
+                    ),
+                )
@@ -4418,0 +4434,13 @@ class GPUModelRunner(
+        from specrhythm.phase4.vllm_diagnostics import capture_target_forward
+
+        capture_target_forward(
+            self,
+            scheduler_output=scheduler_output,
+            logits=logits,
+            spec_decode_metadata=spec_decode_metadata,
+            logits_indices=logits_indices,
+            positions=positions,
+            num_scheduled_tokens=num_scheduled_tokens_np.tolist(),
+            common_attention_metadata=spec_decode_common_attn_metadata,
+        )
+
@@ -4636,0 +4665,16 @@ class GPUModelRunner(
+            if (
+                spec_config is not None
+                and spec_config.method == "custom_class"
+                and scheduler_output.scheduled_spec_decode_tokens
+            ):
+                verify_end_hook = getattr(
+                    self.drafter, "on_target_verify_end", None
+                )
+                if verify_end_hook is not None:
+                    verify_end_hook(
+                        request_ids=req_ids_output_copy,
+                        sampled_token_ids=valid_sampled_token_ids,
+                        scheduled_spec_token_ids=(
+                            scheduler_output.scheduled_spec_decode_tokens.copy()
+                        ),
+                    )
@@ -4948,0 +4993,8 @@ class GPUModelRunner(
+                request_ids=self.input_batch.req_ids.copy(),
+                target_materialized_token_counts=[
+                    int(self.input_batch.num_computed_tokens_cpu[index])
+                    + int(scheduler_output.num_scheduled_tokens[request_id])
+                    for index, request_id in enumerate(
+                        self.input_batch.req_ids
+                    )
+                ],
