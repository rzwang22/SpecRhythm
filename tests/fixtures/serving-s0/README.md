# Synthetic S0 fixtures

Every row in this directory is invented test data. These files are not samples
from the named public datasets and must never be reported as a real-data build.
Dataset-shaped field names exercise the official adapters, including answer fields
that must not enter any prompt. The fixture tokenizer exists only in tests.
